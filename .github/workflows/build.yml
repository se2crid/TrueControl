name: Build Unsigned IPA for iOS (Expo)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Install Expo CLI
        run: npm install -g expo-cli

      - name: Login to Expo (Optional: if you want to use your Expo account)
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: expo login --token $EXPO_TOKEN

      - name: Build unsigned IPA
        run: npx expo run:ios --no-install --device

      # If you want to use EAS Build:
      # - name: Install EAS CLI
      #   run: npm install -g eas-cli
      # - name: Build with EAS
      #   run: eas build -p ios --profile preview --non-interactive

      # - name: Upload artifact
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: unsigned-ipa
      #     path: path/to/your/ipa
